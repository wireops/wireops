package ntfy

// Config field keys, referenced by descriptor.Fields[i].Key in ntfy.go.
const (
	keyURL      = "url"
	keySecret   = "secret"
	keyUser     = "user"
	keyTopic    = "topic"
	keyTemplate = "template"
	keyEvents   = "events"
)
