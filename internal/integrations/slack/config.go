package slack

// Config field keys, referenced by descriptor.Fields[i].Key in slack.go.
const (
	keyURL            = "url"
	keyMentionOnError = "mention_on_error"
	keyMentionText    = "mention_text"
	keyEvents         = "events"
)
